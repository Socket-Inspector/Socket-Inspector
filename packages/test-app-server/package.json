{
  "name": "test-app-server",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.3.5"
  },
  "scripts": {
    "serve": "bun server.ts",
    "watch": "bun --watch server.ts",
    "serve:playwright": "PORT=6857 bun server.ts",
    "serve:test-app-tiny:playwright": "PORT=5890 bun server.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5"
  }
}
